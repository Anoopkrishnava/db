Write a procedure to display the weekday of a specific day.
Procedure

CREATE PROCEDURE `day`(in d date)
BEGIN
declare dat varchar(10);
set dat=Weekday(d);
select d as Date, dat as WeekDay;
END

SQL Query
call day("2024-3-16");
