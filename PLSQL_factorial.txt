Write a procedure to find factorial of a number.
Procedure

CREATE PROCEDURE `fact`(in num int)
BEGIN
declare f int default 1;
declare n1 int;
set n1=num;
while num >0
do
set f=f*num;
set num=num-1;
end while;
select n1 as Number, f as Factorial;
END

SQL Query
call fact(7);
